## Fantasy Match

# deploying

    1. Build
       - yarn run build
    2. Add dist folder
       - git add dist -f
    3. Commit changes

```bash
git commit -m "Adding dist"
git subtree push --prefix dist origin gh-pages
```
